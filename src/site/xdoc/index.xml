<?xml version="1.0" encoding="UTF-8"?>
<document>
    <properties>
        <title>Plugin identitydesk</title>
    </properties>
    <head>
        <!-- Keywords example : "cms, authentication, cms, workflow, seo, collaborative, ..." 
             Use the language of this xdoc -->
        <meta name="keywords" content="" />
    </head>
    <body>
        <section name="Plugin identitydesk">
            <!--
            <p>
                <img src="images/identitydesk.png" />
            </p>
            -->
            <subsection name="Introduction">
                <p>Identity Desk is a front-office plugin, allowing to search, view, create and update identities, following the connected Lutèce user's service contract rules.</p>
            </subsection>
            <subsection name="Configuration">
                <ul>
                    <li>`identitydesk.identitystore.endpoint` : Identity Store backoffice endpoint called for searching/creating/updating identities.</li>
                    <li>`identitydesk.default.client.code` : Default client code sent when calling Identity Store backoffice.</li>
                    <li>`identitydesk.autocomplete.city.endpoint` : Called endpoint for place of birth autocomplete (INSEE).</li>
                    <li>`identitydesk.autocomplete.country.endpoint` : Called endpoint for birth country autocomplete (INSEE).</li>
                    <li>`identitydesk.attribute.order` : Attributes display order in the forms (write comma-separated attribute keys).</li>
                    <li>`identitydesk.search.strict.attributes` : Attributes list to be searched in non-approximate mode (write comma-separated attribute keys).</li>
                </ul>
            </subsection>
        </section>
    </body>
</document>

