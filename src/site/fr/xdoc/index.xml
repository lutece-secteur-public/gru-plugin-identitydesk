<?xml version="1.0" encoding="UTF-8"?>
<document>
    <properties>
        <title>Plugin identitydesk</title>
    </properties>
    <head>
        <!-- Keywords example : "cms, authentication, cms, workflow, seo, collaborative, ..." 
             Use the language of this xdoc -->
        <meta name="keywords" content="plugin, front, IHM, identité" />
    </head>
    <body>
        <section name="Plugin identitydesk">
            <!--
            <p>
                <img src="images/identitydesk.png" />
            </p>
            -->
            <subsection name="Introduction">
                <p>Identity Desk est un plugin front-office, permettant de rechercher, consulter, créer et modifier des identités, dans le respect du contrat de service de l'utilisateur connecté au système Lutèce.</p>
            </subsection>
            <subsection name="Configuration">
                <ul>
                    <li>`identitydesk.identitystore.endpoint` : Endpoint du backoffice Identity Store pour la recherche/création/modification d'identité.</li>
                    <li>`identitydesk.default.client.code` : Code client par défaut envoyé lors des appels au backoffice Idendity Store.</li>
                    <li>`identitydesk.autocomplete.city.endpoint` : Endpoint utilisé pour l'autocomplétion de la ville de naissance (INSEE).</li>
                    <li>`identitydesk.autocomplete.country.endpoint` : Endpoint utilisé pour l'autocomplétion du pays de naissance (INSEE).</li>
                    <li>`identitydesk.attribute.order` : Détermine l'ordre d'affichage des attributs dans les formulaires (clés d'attributs à renseigner séparés par des virgules).</li>
                    <li>`identitydesk.search.strict.attributes` : Détermine les attributs à rechercher en mode non approximé (clés d'attributs à renseigner séparés par des virgules).</li>
                </ul>
            </subsection>
        </section>
    </body>
</document>

